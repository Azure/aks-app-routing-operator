name: Test
on:
  repository_dispatch:
    types: [ok-to-test-command]

env:
  ref: ${{ github.event.client_payload.pull_request.head.sha }} # todo: guarantee this is equal to the sha or use the short sha

jobs:
  unit:
    uses: ./.github/workflows/unit.yaml
    secrets: inherit
    with:
      ref: ${{ vars.ref }}